<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.ortussolutions.bxsites</groupId>
  <artifactId>bxsites-maven-plugin</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>maven-plugin</packaging>

  <name>BX Sites Maven Plugin</name>
  <description>Generate a bx-sites documentation site from a Maven build, with zero prerequisite installs beyond a JDK.</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      core/ is never published to a real, public repository - see the plan.
      During development this plugin resolves it from the local Maven
      repository; run `./gradlew publishToMavenLocal` in core/ first.
      At release time, core's classes are shaded into this plugin's own
      final jar instead (maven-shade-plugin) - not yet wired up, fast-follow.
    -->
    <bxsites.core.version>0.1.0-SNAPSHOT</bxsites.core.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.ortussolutions.bxsites</groupId>
      <artifactId>bxsites-core</artifactId>
      <version>${bxsites.core.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>3.9.9</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <version>3.9.9</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>3.13.1</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-plugin-plugin</artifactId>
        <version>3.13.1</version>
        <configuration>
          <goalPrefix>bxsites</goalPrefix>
        </configuration>
        <executions>
          <execution>
            <id>default-descriptor</id>
            <phase>process-classes</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
      <!--
        Functional/integration tier - the Maven-side mirror of the Gradle
        plugin's TestKit functional tests. Runs a real `mvn bxsites:build`
        against the fixture under src/it/plain-docs-project and asserts a
        real site/index.html comes out. Needs `mvn install` to have already
        put this plugin's own jar (and core's) into the local repo first -
        run via `mvn verify` or `mvn invoker:install invoker:run`.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-invoker-plugin</artifactId>
        <version>3.7.0</version>
        <configuration>
          <cloneProjectsTo>${project.build.directory}/it</cloneProjectsTo>
          <goals>
            <goal>com.ortussolutions.bxsites:bxsites-maven-plugin:${project.version}:build</goal>
          </goals>
        </configuration>
        <executions>
          <execution>
            <id>integration-test</id>
            <goals>
              <goal>install</goal>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
